what is javascript and why we used it ?
- java scipt is logic based progrmming lnguagewhere we can create our code project functionlity in that
- java script is a high level language for build web page 
- now we use the es6(echma script 6) version of java script ,in other word vanila javascript.

VARIABLE:
- Variable is container to store some data
-in java script we have 3 type of variable 
1. Let:- 
  - let is a type of variable whic is used for changeing the variable name .
  - Now these days most of the cases we used let for creating variable .
  -  let is a block scope code so we have been useing let for most of the cases.
2.VAR:- 
   -Var is a type of variable which is also used for changeing the variable latter stage 
   -var is used for oldest browser so now a days we are dont use var most of the time 
3.const:-
   -const is a type of variable where we cant change our data further
   -const means constant to store some data like number integers etc.....

